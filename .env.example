NODE_ENV=development

# Local Postgres via docker-compose
DATABASE_URL=postgres://postgres:postgres@localhost:5432/ticket_concierge

# Port for Next.js dev (Cloud Run will inject PORT in production)
PORT=3000

